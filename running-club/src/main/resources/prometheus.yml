global:
  scrape_interval: 5s # 5초마다 데이터를 수집해라

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus' # 데이터가 나오는 길목
    static_configs:
      - targets: ['host.docker.internal:8080'] # 도커 안에서 호스트 머신의 스프링 부트로 접속